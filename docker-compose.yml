version: '2'
services:
  onboard-client:
    container_name: onboard-client
    build:
      context: .
      dockerfile: ops/docker/Dockerfile
      args:
        - NODE_ENV=development
    image: onboard-client
    ports:
      - '3000:3000'
    volumes:
      - ./src:/opt/app/src:cached
